<%= form_for(work_schedule) do |f| %>
  <% if work_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_schedule.errors.count, "error") %> prohibited this work_schedule from being saved:</h2>

      <ul>
      <% work_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %>
    <%= f.number_field :day %>
  </div>
    <%=current_user%>
  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :start_work %>
    <%= f.time_select :start_work %>
  </div>

  <div class="field">
    <%= f.label :end_work %>        
    <%= f.time_select :end_work %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
